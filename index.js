
function distanceTravelledInBlocks(location){
  return abs(42 - location);
}
const headquater = 42;

function absolute(number){
  if (number < 0){
    return number* -1;
  }
  return number;
}

//distanceFromHqInBlocks: Returns the number of blocks
//from Scuber's headquarters to the pickup location.

function distanceFromHqInBlocks() (headquater){
  //1 block === 264 
  return distanceTravelledInBlocks * 264;
}
//distanceFromHqInFeet: Returns the number of feet from //Scuber's 

function distanceTravelledInFeet (start, end){
  return absolute(end - start) * 264;
}

function calculatesFarePrice(start, destination) {
  if (absolute(destination - start)>400){
    //if the distance is less than 400 feet return 0
    return 0;
  } else if {
    
  }
}
